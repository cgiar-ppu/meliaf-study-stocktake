# E2E test environment — used by Playwright via `vite --mode e2e`
# Demo mode auto-authenticates without Cognito
VITE_DEMO_MODE=true

# Non-routable API URL — un-mocked fetch calls fail loudly
VITE_API_URL=http://localhost:9999
